# Node / TypeScript
node_modules/
dist/
*.tgz
.npmrc
# Not committed: a single-platform lockfile omits other platforms' optional native
# deps (rollup/esbuild) and breaks cross-platform CI installs (npm#4828).
package-lock.json

# Rust
target/
Cargo.lock

# Python
__pycache__/
*.pyc
.venv/
build/
*.egg-info/

# Go
# (binaries) — keep go.sum; ignore built artifacts
bin/

# misc
.DS_Store
*.log
